import { Link } from 'react-router-dom';
import { useAuth } from '../lib/auth.jsx';
import { infoItems } from '../content/prismTexts.js';
function Header(){const {user}=useAuth();const label=user?.name||user?.email?.split('@')[0]||'Perfil';return <header className="site-header"><Link to="/" className="site-brand"><span className="site-mark"/>PRISM IA</Link><nav><Link className="active" to="/informacoes">Informações</Link><Link to="/modelos">Modelos</Link><Link to="/termos">Termos</Link></nav>{user?<Link to="/chat" className="site-account"><span>{label.slice(0,2).toUpperCase()}</span>{label}</Link>:<Link to="/login" className="site-login">Login</Link>}</header>}
export default function Info(){return <div className="editorial-page brutal-library"><Header/><main><section className="page-intro"><div className="eyebrow">02 / INFORMAÇÕES</div><h1>O que existe<br/>por trás da<br/><em>Prism.</em></h1><p className="lead">Cada bloco abaixo é uma página própria. Abra, leia e volte sem perder o lugar.</p></section><section className="library-list">{infoItems.map(item=><Link to={`/informacoes/${item.id}`} className="library-row" key={item.id}><span>{item.n}</span><div><h2>{item.title}</h2><p>{item.summary}</p></div><b>↗</b></Link>)}</section></main></div>}
