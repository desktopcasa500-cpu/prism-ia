import { Link } from 'react-router-dom';
import { infoItems } from '../content/prismTexts.js';
import PrismIcon from '../components/PrismIcon.jsx';
import { useAuth } from '../lib/auth.jsx';
export default function InfoV2(){const {user}=useAuth();return <div className="prism-library-page"><header className="prism-library-topbar"><Link className="prism-library-brand" to="/chat"><img src="/prism-logo.svg" alt=""/>Prism IA</Link><nav className="prism-library-nav"><Link className="active" to="/informacoes">Informações</Link><Link to="/modelos">Modelos</Link><Link to="/termos">Termos</Link><Link to="/chat">Chat</Link><Link to="/codex">Codex</Link></nav><Link className="prism-library-account" to="/configuracoes"><span className="avatar">{String(user?.name||'P').slice(0,1).toUpperCase()}</span>{user?.name||'Conta'}</Link></header><main className="prism-library-main"><section className="prism-library-hero"><div className="prism-library-eyebrow">Informações / Plataforma</div><h1>Como a Prism<br/><em>funciona por dentro.</em></h1><p>Arquitetura, modelos, esforço, ferramentas e execução. Cada assunto tem uma página própria e uma explicação direta.</p></section><div className="prism-library-list">{infoItems.map((item)=><Link className="prism-library-row" to={`/informacoes/${item.id}`} key={item.id}><span className="index">{item.n}</span><div><h2>{item.title}</h2><p>{item.summary}</p></div><span className="arrow"><PrismIcon name="layers" size={14}/></span></Link>)}</div></main></div>}
