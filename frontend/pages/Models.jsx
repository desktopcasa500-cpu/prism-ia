import { Link } from 'react-router-dom';
import { useAuth } from '../lib/auth.jsx';
import { modelItems } from '../content/prismTexts.js';
function Header(){const {user}=useAuth();const label=user?.name||user?.email?.split('@')[0]||'Perfil';return <header className="site-header"><Link to="/" className="site-brand"><span className="site-mark"/>PRISM IA</Link><nav><Link to="/informacoes">Informações</Link><Link className="active" to="/modelos">Modelos</Link><Link to="/termos">Termos</Link></nav>{user?<Link to="/chat" className="site-account"><span>{label.slice(0,2).toUpperCase()}</span>{label}</Link>:<Link to="/login" className="site-login">Login</Link>}</header>}
export default function Models(){return <div className="editorial-page brutal-library"><Header/><main><section className="page-intro models-intro"><div className="eyebrow">03 / MODELOS</div><h1>Uma rota para<br/>cada tipo de<br/><em>trabalho.</em></h1><p className="lead">Os nomes Prism representam configurações de orquestração. Abra qualquer modelo para entender exatamente para que ele foi pensado.</p></section><section className="library-list model-library">{modelItems.map(item=><Link to={`/modelos/${item.id}`} className="library-row" key={item.id}><span>{item.n}</span><div><h2>{item.name}</h2><p>{item.summary}</p></div><small>{item.level}</small><b>↗</b></Link>)}</section><section className="ultra"><span>ULTRACODE</span><h2>Três motores.<br/>Uma resposta.</h2><p>Ultracode é o nível mais alto de orquestração: raciocínio, geração e validação acontecem simultaneamente antes da síntese final.</p></section></main></div>}
