import { Link, useParams } from 'react-router-dom';
import { infoItems, modelItems } from '../content/prismTexts.js';
import PrismIcon from '../components/PrismIcon.jsx';
export default function PrismDetailV2({type}){const {id}=useParams();const list=type==='models'?modelItems:infoItems;const item=list.find((entry)=>entry.id===id);if(!item)return <div className="prism-detail-page"><main className="prism-detail-main"><Link className="prism-detail-back" to={type==='models'?'/modelos':'/informacoes'}>← voltar</Link><h1>Não encontrado.</h1></main></div>;const paragraphs=String(item.body||item.summary||'').split(/\n\n+/);return <div className="prism-detail-page"><header className="prism-library-topbar"><Link className="prism-library-brand" to="/chat"><img src="/prism-logo.svg" alt=""/>Prism IA</Link><nav className="prism-library-nav"><Link to="/informacoes">Informações</Link><Link className={type==='models'?'active':''} to="/modelos">Modelos</Link><Link to="/termos">Termos</Link><Link to="/chat">Chat</Link><Link to="/codex">Codex</Link></nav></header><main className="prism-detail-main"><Link className="prism-detail-back" to={type==='models'?'/modelos':'/informacoes'}><PrismIcon name="layers" size={12}/> voltar à biblioteca</Link><div className="prism-detail-index">{item.n} / {type==='models'?item.level:'INFORMAÇÃO'}</div><h1>{item.title||item.name}</h1><p className="prism-detail-summary">{item.summary}</p><div className="prism-detail-rule"/><div className="prism-detail-copy">{paragraphs.map((paragraph,index)=><p key={index}>{paragraph}</p>)}</div><Link className="prism-detail-next" to={type==='models'?'/chat':'/informacoes'}>{type==='models'?'Usar e voltar ao Chat':'Voltar à biblioteca'} →</Link></main></div>}
