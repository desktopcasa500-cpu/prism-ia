import { Link } from 'react-router-dom';
import { useAuth } from '../lib/auth.jsx';
import PrismIcon from '../components/PrismIcon.jsx';
const TOPICS=[['01','Roteamento','Como os provedores são escolhidos e usados.','roteamento'],['02','Motores externos','Gemini, Groq, OpenAI, Anthropic, NVIDIA e camadas intermediárias.','motores'],['03','OpenRouter','Quando a camada intermediária participa do fluxo.','openrouter'],['04','Orquestração','Ferramentas, fallback, validação e síntese.','orquestracao'],['05','Créditos e tokens','Como o uso é contabilizado por execução.','creditos'],['06','Privacidade','Isolamento de conta, projetos e credenciais.','privacidade'],['07','Limites','Regras de uso, rate limiting e planos.','limites']];
export default function Terms(){const {user}=useAuth();return <div className="prism-library-page"><header className="prism-library-topbar"><Link className="prism-library-brand" to="/chat"><img src="/prism-logo.svg" alt=""/>Prism IA</Link><nav className="prism-library-nav"><Link to="/informacoes">Informações</Link><Link to="/modelos">Modelos</Link><Link className="active" to="/termos">Termos</Link><Link to="/chat">Chat</Link><Link to="/codex">Codex</Link></nav><Link className="prism-library-account" to="/configuracoes"><span className="avatar">{String(user?.name||'P').slice(0,1).toUpperCase()}</span>{user?.name||'Conta'}</Link></header><main className="prism-library-main"><section className="prism-library-hero"><div className="prism-library-eyebrow">Documentação / Prism IA</div><h1>Sem parede de texto.<br/><em>Uma regra por vez.</em></h1><p>Detalhes operacionais da plataforma, organizados para consulta rápida antes de uma execução ou integração.</p></section><div className="prism-library-list">{TOPICS.map(([n,title,summary,slug])=><Link className="prism-library-row" to={`/termos/${slug}`} key={slug}><span className="index">{n}</span><div><h2>{title}</h2><p>{summary}</p></div><span className="arrow"><PrismIcon name="layers" size={14}/></span></Link>)}</div></main></div>}
